{
  "name": "target",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply millisecond-target-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply millisecond-target-db --remote"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250719.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.4.0"
  }
}
